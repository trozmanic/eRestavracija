const mongoose=require("mongoose");
const Gost=mongoose.model("Gost");

const pridobiGoste=(req,res)=>{}

const pridobiGosta=(req,res)=>{}

const ustvariGosta=(req,res)=>{}

module.exports={
    pridobiGoste,
    pridobiGosta,
    ustvariGosta
}